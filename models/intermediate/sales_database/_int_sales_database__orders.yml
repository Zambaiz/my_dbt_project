version: 2

models:
  - name: int_sales_db__orders
    description: '{{ doc("int_sales_db__orders") }}'
    columns:
      - name: order_id
        description: "Primary key, unique identifier for each order."
        tests:
          - unique
          - not_null
