version: 2

models:
  - name: int_sales_db__users
    description: '{{ doc("int_sales_db__users") }}'
    columns:
      - name: user_id
        description: "Primary key, unique identifier for each user."
        tests:
          - unique
          - not_null
